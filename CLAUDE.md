# CLAUDE.md

このリポジトリで作業する AI エージェント（Claude Code / Codex など）向けの入口。

## 交代したエージェントへ — まずこの3つを読む

1. **[docs/STATUS.md](docs/STATUS.md)** — いまどこまで出来ているか。**実機で確認したものと
   JVM テストだけのものを分けて書いてある**。ここが最新の真実
2. **[docs/PLAN.md](docs/PLAN.md)** — 何を作ろうとしているか。決定13件・段階0〜6・実測値・リスク
3. **[AGENTS.md](AGENTS.md)** — 禁止事項と規約

そのあと用語集の [CONTEXT.md](CONTEXT.md) に目を通す。**「稜線」と「尾根線」は別物**で、
取り違えると設計が崩れる。

## このアプリ

**峰ミル** — SABERA スマートグラスに**山の稜線と山名を重ねる** Android アプリ。
平地から遠景を見る想定（鯖江から白山、甲府から富士山）。山頂での利用は対象外。

姉妹アプリの**星しるべ**（星座版）が
`/Users/kaito/Documents/jigintern/sabera-team-e` にあり、**そこから資産を借りている**。
借りているものの一覧は [docs/PLAN.md](docs/PLAN.md) の「使う既存資産」。

星と山は座標系以外ほぼ何も共有しない。**星は無限遠で互いを隠さない。山は有限距離で互いを隠す。**
この差が設計の大半を決めている。

## 前回の実装は消えている（重要）

2026-08-27 の午前まで、このディレクトリには 517 テスト合格の実装があったが、
**git 未追跡のまま消去された**。セッションログから復元したものが

```
~/Documents/jigintern/minemiru-salvage/recovered/
```

にある（Kotlin 16・テスト10・Python ツール4）。**このディレクトリを消さないこと。**

使い方は決定#3 で決まっている：

- **`tools/*.py` は復元物をそのまま使う**（国土地理院の仕様実装なので書き直す価値が薄い）
- **`src/test/**` は復元物を仕様書として使う**（何が正しいかの記録）
- **`src/main/**` の Kotlin は参考にとどめ、自分で書く**

## 開発コマンド

```bash
./gradlew :app:installDebug              # 実機にインストール（BLE 必須。エミュレータ不可）
./gradlew :app:testDebugUnitTest         # JVM テスト
python3 tools/build-dem.py               # 同梱 DEM を作る（全国 z10・外部取得あり）
python3 tools/build-peak-catalog.py      # data/peaks.json を作る
```

**`./gradlew :app:lintDebug` は叩かない。** AGP 8.7.0 と Kotlin 2.3.10 の非互換で
クラッシュする（星しるべで既知）。detector を無効化して通すのも禁止。

## 作業を中断するとき

利用上限やセッション切れで止まるときは、**止まる前に必ず**：

1. 書きかけでもコミットする（動かなくてよい。`wip:` で構わない）
2. `docs/STATUS.md` の「いまどこ」「次にやること」「詰まっていること」を埋める
3. **実機未確認のものを「動く」と書き換えない**

前回はこれが無かったので、517 テストが通る実装があったこと自体、
消えた後にセッションログを掘るまで誰にも分からなかった。

# 進捗

最終更新: 2026-08-27 14:30

**このファイルが最新の真実。** 実機で確認したものと JVM テストだけのものを必ず分けて書く。
[AGENTS.md](../AGENTS.md) の「確かめていないことを『動く』と書かない」を守ること。

## いまどこ

段階0（サルベージと引き継ぎ文書）が完了。段階1（DEM 取得）を走らせている最中。
アプリのコードはまだ1行も無い。

## 段階ごとの状態

| 段階 | 内容 | 状態 | 確認方法 |
|---|---|---|---|
| 0 | サルベージ・git init・引き継ぎ文書 | ✅ 完了 | `git log` |
| 1 | DEM 全国 z10 取得・`data/peaks.json` | 🔄 **DEM 取得中**・peaks 未着手 | `python3 tools/build-dem.py --verify` |
| 2 | 空プロジェクトを立てる（フラット構成） | ⬜ 未着手 | グラスに画像1枚 |
| 3 | `geo/` `tile/` `terrain/` を TDD で | ⬜ 未着手 | `./gradlew :app:testDebugUnitTest` |
| 4 | **稜線をグラスに出す** 🎯 絶対防衛線 | ⬜ 未着手 | 実機 |
| 5 | 山名ラベル7枠 | ⬜ 未着手 | 実機 |
| 6 | 稜線合わせ・画角の実測 | ⬜ 未着手 | 実機 |

凡例 — ✅ 完了 / 🔄 途中 / ⬜ 未着手。
完了のうち**実機で確認したもの**には「**実機確認済み**」と明記する。書いていなければ未確認。

## 次にやること

1. `tools/build-dem.py` の完走を待つ（全国 z10・約581枚・18MB 見込み）。
   **終わったら真っ先にコミットする** — 581枚の再取得が最も高コスト
2. `python3 tools/build-peak-catalog.py` で `data/peaks.json` を作る
   （`~/Documents/jigintern/minemiru-salvage/tmp/**/scratchpad/` に
   `hyaku_resolved.json` と `1003zan20260331.geojson` が生存しているので再取得は不要）
3. 段階2 — 星しるべ（`/Users/kaito/Documents/jigintern/sabera-team-e`）から
   ビルド足場と SDK 起動まわりを持ってきてフラット構成に直す

## 詰まっていること

無し。

## 決まっていないこと

- `ui/` をどこまで星しるべから持ってくるか。`CalibrationScreen`(859行)
  `GlassesApp`(555行) `ui/component/`(2,953行) が絡んでおり、**明日の時間が溶けるとしたらここ**。
  段階4の稜線表示は最小の画面で先に出し、UI の本格移植は段階6でまとめる方針

## 検証済みの事実（環境）

| 項目 | 状態 | 確認日 |
|---|---|---|
| SDK の PAT | `~/.gradle/gradle.properties` に設定済み | 2026-08-27 |
| SDK 0.6.0 の AAR | Gradle キャッシュに存在（`--offline` でも解決可能） | 2026-08-27 |
| 実機のファーム | **画像表示に対応**（星しるべの星図が実機で出ている） | 2026-08-27・ユーザー確認 |
| `:app:lintDebug` | **クラッシュする**（AGP 8.7.0 × Kotlin 2.3.10） | 星しるべで既知 |

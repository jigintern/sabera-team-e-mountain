# AGENTS.md

峰ミルで作業する AI エージェント向けの共通ガイド。**ここは禁止事項と規約だけ。**
いまどこまで出来ているかは [docs/STATUS.md](docs/STATUS.md)、
何を作るかは [docs/PLAN.md](docs/PLAN.md)、言葉の定義は [CONTEXT.md](CONTEXT.md)。

## このアプリ

- **SABERA** = スマートグラス。**峰ミル** = そこに**山の稜線と山名**を重ねるアプリ
- シナリオ = **平地から遠景を見る**（鯖江から白山）。山頂利用は対象外
- 姉妹アプリ **星しるべ**（星座版）= `/Users/kaito/Documents/jigintern/sabera-team-e`。
  **借りている資産の一覧は [docs/PLAN.md](docs/PLAN.md)**

## 原則

- **頼まれていないアプリ機能を先回りして実装しない**
- **未決定事項を勝手に埋めない。** [docs/PLAN.md](docs/PLAN.md) に追記するか質問する
- **実機（BLE）でしか確かめられないことが多い。確かめていないことを「動く」と書かない**
- **隠れた山は描かない。** 手前の地形に遮られた山を描くのが最悪の振る舞い。
  星と違い山は互いを隠すので、これが設計の中心にある
- **決まったことも踏んだ失敗もリポジトリに残す。** チャットで終わらせない。
  **前回はこれを怠って実装ごと失われた**

## やってはいけないこと

| やらない | なぜ |
|---|---|
| 確かめていないことを「動く」と書く | 実機とテストの区別が消えると、次の人が実機確認を飛ばす |
| **コミットせずに作業を進める** | **前回 517 テストが通る実装が未追跡のまま消えた。これが最大の教訓** |
| `data/**` を直接編集する | すべて生成物。次の生成で消える |
| 塗りつぶしで山を描く | 黒＝透明の波導ディスプレイ。光った画素は**実物の山を隠す** |
| 細い線で描く | 屋外の空に負ける。**最上段の階調(255)だけを使い線幅を稼ぐ** |
| `yawDegrees` をそのまま方位に使う | 磁力計が無く、静止中も **44°/分**流れる。`YawDriftCorrector` を通す |
| 画像を「枚数」で設計する／回るものを画像で描く | 先に尽きるのはバッファ。全画面1枚は 332〜390ms かかり点滅になる |
| 絵と根拠を別々に計算する | 星しるべで「オリオン座」と出しながら別の星座を喋った（#37）。`RidgeMap` が1つの容器で運ぶ |
| 首の向きを命令に使う | 稜線を出している間は**頭の向き＝見ている方角** |
| RLE / Opus のコーデックを自前で書く | SDK に入っている |
| `./gradlew :app:lintDebug` を叩く | AGP 8.7.0 と Kotlin 2.3.10 の非互換でクラッシュする。detector 無効化での回避も禁止 |
| `Co-Authored-By` にエージェントを入れる | コミットの作者は人間 |
| 秘密情報（PAT・API キー）をコミットする | `.env` は `.gitignore` 済み |

## 外せない数値

**正本は [docs/team-e/76_terrain-measurements.md](docs/team-e/76_terrain-measurements.md)。**
ここは早見表で、食い違ったら台帳のほうが正しい。

| | |
|---|---|
| パネル | **576×360**。画像1枚は **544×340 を試し、入らなければ 528×330** |
| 色 | **緑8階調（3bit）。黒は透明** |
| 画像バッファ | **380,000 バイト**（`width * height * 2` ＋圧縮後サイズ） |
| テキスト | **8要素・合計 190 バイト**（画面全体の合計） |
| 転送 | 528×330 の1枚で **332〜390ms** |
| 描き直し | **0.18 秒静止 ＋ 前の絵から 6° 以上** |
| 画角 | **仮の 35°・未実測**（`STAR_MAP_FOV_DEG`）。段階6の較正で実測して埋める |
| 見通しの範囲 | **半径 150km**。200km では落ち込み 2,732m でほぼ何も残らない |
| 落ち込み | `drop(d) = 0.87 × d² / (2 × 6,371,000)`（屈折係数 k=0.13） |
| 焼き直しの閾値 | **50m 移動**。50km 先の山が1画素(0.066°)動く距離が約 57m |
| DEM | 全国 **z10・画素 124m・4m 量子化・約581枚・18MB** |
| 検算値 | 鯖江(35.9432, 136.1846, 50m) → 白山 御前峰(2702m, 57.8km) = **仰角 2.40°** |
| 検算値 | 鯖江 → 日野山(794m, 9.6km) = **仰角 4.41°**（白山より高く見える） |
| ヨードリフト | 静止中 **−44°/分**。`YawDriftCorrector` 込みで実測 0.0°/分 |

## 精度の予算

| 用途 | 要求精度 |
|---|---|
| 解説する対象を決める | ±20° |
| ラベルを方向に置く | ±5〜10° |
| **稜線を実景に重ねる** | **±2〜3°** ← 峰ミルはここに居る |

星しるべは天体アライメントを実装後 2026-08-21 に「精度より UX」で**意図的に削除**した。
**彼らが要らないと判断して捨てた精度が、こちらでは必須。**

## 規約

- コミット = `<type>: <日本語の要約>`。type は `feat:` `fix:` `docs:` `refactor:` `test:` `chore:` `build:`
- **`Co-Authored-By` にエージェントを入れない**
- ドキュメント・コミットメッセージは**日本語**
- コードのコメントは「何をしているか」ではなく「なぜそうしたか」
- **ドキュメントは箇条書きと表で書く。この AGENTS.md は 200 行を超えない**
- SDK API の説明は複製せず、[上流の公開ドキュメント](https://jig-sabera.github.io/sabera-sdk/)を参照する
- ライセンス — 出典表示は**アプリ内に出す**。コードから生成し手で二重管理しない
